apiVersion: v1
kind: Secret
metadata:
  name: db-secret
  namespace: microservice-demo
type: Opaque
stringData:
  # Keep credentials out of images and manifests.
  DB_USER: appuser
  DB_PASSWORD: supersecret

